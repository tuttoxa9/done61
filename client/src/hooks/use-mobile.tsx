/**
 * Хук для определения мобильных устройств с кэшированием результата
 * ВАЖНО: Функционал отключен для сохранения визуальных эффектов (блюр, анимации) на всех устройствах
 * @param mobileWidth Ширина экрана, ниже которой устройство считается мобильным (по умолчанию 768px)
 * @returns Всегда возвращает false для показа анимаций и эффектов на всех устройствах
 */
export function useMobile(mobileWidth = 768): boolean {
  // Всегда возвращаем false, чтобы на всех устройствах показывать эффекты и анимации как на десктопе
  return false;
}

export const useIsMobile = useMobile;
export default useMobile;
